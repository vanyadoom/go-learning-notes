# Go с нуля до собеседования 🐹

Мой личный путь изучения языка Go: теория, практические задачи, поиск ошибок, разбор кода, Git/GitHub и подготовка к собеседованию.

Цель проекта — не просто запомнить синтаксис Go, а научиться **самостоятельно писать, читать и отлаживать код**, понимать причины ошибок и постепенно перейти от учебных задач к реальным backend-проектам.

---

## 🎯 Цель проекта

Путь обучения:

```text
Go с нуля
    ↓
базовый синтаксис
    ↓
управляющие конструкции
    ↓
функции
    ↓
struct и методы
    ↓
ошибки и указатели
    ↓
структуры данных
    ↓
interfaces
    ↓
concurrency
    ↓
HTTP / REST API
    ↓
PostgreSQL / SQL
    ↓
тестирование
    ↓
Docker / CI
    ↓
реальный backend-проект
    ↓
подготовка к собеседованию
```

---

# 📚 Как проходит обучение

Каждая тема проходит несколько этапов:

1. 🧠 Теория.
2. 🎯 Самостоятельная практическая задача.
3. 🔎 Разбор решения и ошибок.
4. 🐛 Поиск ошибок в чужом коде.
5. 📝 Фиксация важных моментов в конспекте.
6. 🐙 Отдельный Git-коммит для законченного изменения.
7. ☁️ Push результата на GitHub.

Главный принцип:

> **Сначала пытаюсь решить самостоятельно. Готовое решение использую только после разбора собственных ошибок.**

---

# 📊 Текущий прогресс

**Текущий этап:** повторение изученного материала и работа над ошибками после контрольной.

### Контрольная работа

```text
Результат: 60 / 100
```

Контрольная включала:

* теорию;
* чтение и выполнение кода;
* поиск логических ошибок;
* `struct`;
* методы;
* `error`;
* value / pointer receiver;
* написание программы с нуля;
* Git.

### Bug Hunt

```text
🐛 Выполнено: 20 / 20
```

В заданиях встречались как реальные ошибки, так и **Attention Check** — полностью корректный код.

Основные закреплённые темы:

```text
if / else / else if
switch
for
break
fmt.Scan
error
return
счётчики
накопители
области видимости
struct
методы
value receiver
pointer receiver
```

---

# 🧠 Пройденные темы

| Тема                                       | Статус |
| ------------------------------------------ | :----: |
| Структура программы Go                     |    ✅   |
| Переменные и базовые типы                  |    ✅   |
| Нулевые значения                           |    ✅   |
| Арифметические операции                    |    ✅   |
| `fmt.Println`                              |    ✅   |
| `fmt.Scan`                                 |    ✅   |
| `&` и адрес переменной                     |   🟡   |
| `error` / `nil`                            |   🟡   |
| `return`                                   |   🟡   |
| `if / else / else if`                      |    ✅   |
| Операторы сравнения                        |    ✅   |
| Логические операторы                       |    ✅   |
| `switch`                                   |    ✅   |
| `for`                                      |    ✅   |
| `break`                                    |    ✅   |
| Счётчики и накопители                      |   🟡   |
| Диапазоны                                  |    ✅   |
| Обмен значений                             |    ✅   |
| Область видимости                          |    ✅   |
| Функции                                    |    ✅   |
| Параметры и аргументы                      |   🟡   |
| Несколько возвращаемых значений            |    ✅   |
| `struct`                                   |    ✅   |
| Создание структур                          |    ✅   |
| Методы                                     |    ✅   |
| Value receiver                             |    ✅   |
| Pointer receiver                           |   🟡   |
| Методы, изменяющие состояние               |   🟡   |
| Метод + `error`                            |    ✅   |
| Проверка данных перед изменением состояния |   🟡   |

`🟡` означает: тема уже изучалась, но требует дополнительного закрепления.

---

# 🏋️ Практика

Практические задачи хранятся в отдельных директориях:

```text
practice/

├── 01-.../
├── 02-.../
├── 03-.../
├── ...
├── 07-sum-1-to-n/
├── ...
├── 10-switch/
├── ...
├── 16-calculator/
├── 17-student-info/
├── 18-methods/
├── 19-struct-methods/
├── 20-student-status/
├── 21-bank-account/
├── 22-user-profile/
├── 23-control-loop/
└── 24-control-student/
```

Нумерация отражает последовательность прохождения учебной программы.

---

## ✅ Выполненные практические задачи

### Базовые задачи

```text
Сумма двух чисел
Ввод данных
Проверка возраста
Возрастные категории
Таблица умножения
Чётные и нечётные числа
Сумма от 1 до n
```

В этих задачах закреплялись:

```text
переменные
int
string
арифметика
fmt.Println
fmt.Scan
&
error
if / else
else if
for
%
счётчики
накопители
```

### Последующие задачи

#### `practice/10-switch`

Работа с:

```text
switch
default
условиями
```

#### `practice/16-calculator`

Калькулятор с функцией:

```text
calculate(a, b, operation)
```

Закреплено:

```text
функции
параметры
несколько возвращаемых значений
error
switch
проверка деления на ноль
```

#### `practice/17-student-info`

Работа со структурой:

```go
Student
```

Закреплено:

```text
struct
поля
создание структуры
функция создания объекта
```

#### `practice/18-methods`

Закреплены методы:

```text
IsAdult
IsGradeAtLeast
SetGrade
SetAge
```

#### `practice/19-struct-methods`

Продолжение работы со:

```text
struct
методами
receiver
изменением состояния
```

#### `practice/20-student-status`

Комплексная работа со студентом:

```text
Student
методы
SetGrade
error
CanGraduate
Status
IsExcellent
```

Особое внимание:

```text
проверка данных
↓
изменение состояния
```

#### `practice/21-bank-account`

Структура:

```go
BankAccount
```

Методы:

```text
Deposit
Withdraw
```

Закреплено:

```text
валидация
error
изменение состояния
pointer receiver
```

#### `practice/22-user-profile`

Структура:

```go
User
```

Методы:

```text
IsAdult
Activate
Deactivate
Status
```

Закреплено:

```text
методы
bool
изменение состояния
value / pointer receiver
```

#### `practice/23-control-loop`

Контрольная задача на:

```text
for {}
fmt.Scan
break
error
счётчик
накопитель
if / else
```

#### `practice/24-control-student`

Контрольная задача на:

```text
struct
функцию создания
методы
receiver
SetGrade
error
Status
IsAdult
```

---

# 🐛 Bug Hunt & Code Review

Отдельный формат практики:

> **Сначала определить, действительно ли код содержит ошибку. Только после этого искать исправление.**

В заданиях встречались ошибки:

```text
неправильный порядок вычислений
неправильный receiver
отсутствующий return
бесконечный цикл
неправильная работа со счётчиком
ошибочное накопление суммы
изменение состояния до валидации
использование старого результата
```

Также были задания, где:

```text
код полностью корректен
```

Это важно, потому что цель — не искать ошибку любой ценой, а **понимать поведение программы**.

---

# 📝 Конспекты

Теоретические материалы хранятся в:

```text
notes/
```

Главный сводный конспект:

```text
notes/all.md
```

В нём постепенно собираются изученные конструкции Go и основные ошибки, которые встречались во время практики.

---

# 📈 Что сейчас требует дополнительного закрепления

Контрольная показала несколько слабых мест:

```text
&
error / nil
return
%
break vs return
count vs sum
параметры vs аргументы
value receiver vs pointer receiver
метод vs обычная функция
проверка данных до изменения состояния
Git staging
```

Поэтому перед переходом к следующим темам выполняется отдельный блок:

```text
работа над ошибками
    ↓
повторение теории
    ↓
маленькие упражнения
    ↓
повторный Bug Hunt
    ↓
самостоятельная контрольная практика
```

---

# 🐙 Git и GitHub

Git используется не только для хранения проекта, но и как часть процесса обучения.

Главный принцип:

> **Одно законченное логическое изменение → один отдельный коммит.**

Например:

```text
feat: add calculator
feat: add student struct
feat: add student methods
feat: add bank account
feat: add user profile
docs: update go notes
chore: update gitignore
```

Не использовать один огромный коммит для нескольких независимых изменений.

---

## Рабочий Git-процесс

После завершения отдельной задачи:

```powershell
git status
git add .\practice\XX-task\main.go
git diff --cached
git commit -m "feat: add ..."
git push origin main
git status
```

Важно добавлять в staging только те файлы, которые относятся к текущему изменению.

---

# 🗂️ Структура репозитория

```text
go-learning-notes/
│
├── README.md
├── .gitignore
├── go.mod
│
├── notes/
│   ├── 00-programma-kursa.md
│   ├── 03-upravlyayushchie-konstrukcii.md
│   ├── all.md
│   └── ...
│
└── practice/
    ├── 01-.../
    │   └── main.go
    ├── ...
    ├── 10-switch/
    │   └── main.go
    ├── ...
    ├── 16-calculator/
    │   └── main.go
    ├── 17-student-info/
    │   └── main.go
    ├── 18-methods/
    │   └── main.go
    ├── 19-struct-methods/
    │   └── main.go
    ├── 20-student-status/
    │   └── main.go
    ├── 21-bank-account/
    │   └── main.go
    ├── 22-user-profile/
    │   └── main.go
    ├── 23-control-loop/
    │   └── main.go
    └── 24-control-student/
        └── main.go
```

---

# 🧭 Дорожная карта

Следующие этапы обучения:

|  № | Тема                               | Статус |
| -: | ---------------------------------- | :----: |
| 01 | Введение в Go, установка, `go.mod` |    ✅   |
| 02 | Переменные, типы, константы        |   🟡   |
| 03 | `if`, `else`, `switch`, `for`      |    ✅   |
| 04 | Функции                            |    ✅   |
| 05 | Указатели                          |   🟡   |
| 06 | Массивы и слайсы                   |    ⬜   |
| 07 | Мапы                               |    ⬜   |
| 08 | Структуры и методы                 |    ✅   |
| 09 | Интерфейсы                         |    ⬜   |
| 10 | Обработка ошибок                   |   🟡   |
| 11 | Пакеты и модули                    |   🟡   |
| 12 | Горутины и каналы                  |    ⬜   |
| 13 | `select` и `sync`                  |    ⬜   |
| 14 | Конкурентность и race conditions   |    ⬜   |
| 15 | Дженерики                          |    ⬜   |
| 16 | Стандартная библиотека             |    ⬜   |
| 17 | JSON и `net/http`                  |    ⬜   |
| 18 | Тестирование                       |    ⬜   |
| 19 | Идиомы и инструменты Go            |    ⬜   |
| 20 | Собеседование и практика           |    ⬜   |

---

# 🚀 Следующий большой этап

После полного закрепления текущего материала планируется перейти к следующим фундаментальным структурам данных Go:

```text
arrays
    ↓
slices
    ↓
maps
    ↓
strings / bytes / runes
    ↓
packages / modules
    ↓
interfaces
```

После этого:

```text
goroutines
channels
select
sync
context
```

Затем backend-часть:

```text
HTTP
REST
JSON
PostgreSQL
SQL
testing
Docker
GitHub Actions
```

И уже после этого — полноценный учебный backend-проект.

---

# 🎯 Главный принцип обучения

Цель — не просто получить работающий код.

Важно понимать:

```text
почему код работает
↓
что происходит во время выполнения
↓
где возникает ошибка
↓
почему она возникает
↓
как найти её самостоятельно
↓
как исправить
↓
как сделать код понятнее
```

Каждая ошибка — часть обучения.

Каждый практический проект — часть портфолио.

Каждый Git-коммит — часть истории развития проекта.

---

# 🐹 Финальная цель

**Go с нуля → уверенное владение языком → структуры данных → interfaces → concurrency → HTTP/API → SQL/PostgreSQL → тестирование → Docker → реальные backend-проекты → подготовка к собеседованию Go-разработчика.**

Этот репозиторий — пошаговая история этого пути.
